# QSR Digest — managed-agent cookbook
# LK Group / Daniels Donuts weekly intelligence digest
# Agent context: ./agents/qsr-digest-orchestrator.md
# Knowledge base: ./knowledge-base/ (injected at runtime by run-single.py)

name: qsr-digest
model: claude-opus-4-7

system:
  file: ./agents/qsr-digest-orchestrator.md

tools:
  - type: agent_toolset_20260401
    default_config: { enabled: false }
    configs:
      - { name: web_search, enabled: true }
      - { name: bash,       enabled: true }
      - { name: write,      enabled: true }
      - { name: read,       enabled: true }
